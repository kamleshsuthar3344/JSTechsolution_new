-- Database Migration Script for Earning Sharthi Partnership v2

-- 1. Create Admin Users Table
CREATE TABLE IF NOT EXISTS `admin_users` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `email` varchar(100) NOT NULL,
  `password` varchar(255) NOT NULL,
  `full_name` varchar(100) NOT NULL,
  `role` varchar(20) DEFAULT 'admin',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 2. Create Referral Users Table
CREATE TABLE IF NOT EXISTS `referral_users` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `email` varchar(100) NOT NULL,
  `password` varchar(255) NOT NULL,
  `full_name` varchar(100) NOT NULL,
  `referral_code` varchar(50) NOT NULL,
  `wallet_balance` decimal(10,2) DEFAULT '0.00',
  `status` enum('active','blocked','pending') DEFAULT 'active',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`),
  UNIQUE KEY `referral_code` (`referral_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. Create Affiliate Users Table
CREATE TABLE IF NOT EXISTS `affiliate_users` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `email` varchar(100) NOT NULL,
  `password` varchar(255) NOT NULL,
  `full_name` varchar(100) NOT NULL,
  `affiliate_id` varchar(50) NOT NULL,
  `commission_rate` decimal(5,2) DEFAULT '15.00',
  `status` enum('active','blocked','pending') DEFAULT 'active',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`),
  UNIQUE KEY `affiliate_id` (`affiliate_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 4. Create Services Master Table
CREATE TABLE IF NOT EXISTS `services` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL,
  `slug` varchar(100) NOT NULL,
  `description` text,
  `page_url` varchar(255) NOT NULL,
  `icon_name` varchar(50) DEFAULT 'Monitor',
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Insert Default Services
INSERT INTO `services` (`name`, `slug`, `page_url`, `icon_name`) VALUES
('Website Development', 'website-development', 'https://earningsharthi.in/website-development', 'Monitor'),
('Digital Marketing', 'digital-marketing', 'https://earningsharthi.in/digital-marketing', 'Megaphone'),
('SEO Services', 'seo-services', 'https://earningsharthi.in/seo-services', 'Search'),
('Shopify Development', 'shopify-development', 'https://earningsharthi.in/shopify-development', 'ShoppingBag'),
('Wordpress Website', 'wordpress-development', 'https://earningsharthi.in/wordpress-development', 'Globe'),
('UI/UX Design', 'ui-ux-design', 'https://earningsharthi.in/ui-ux-design', 'PenTool'),
('Backlink Services', 'backlink', 'https://earningsharthi.in/backlink', 'Link'),
('Growth Services', 'growth-services', 'https://earningsharthi.in/growth-services', 'Rocket'),
('App Development', 'app-development', 'https://earningsharthi.in/services', 'Smartphone');

-- 5. Create Service Referrals / Tracking Table
CREATE TABLE IF NOT EXISTS `service_referrals` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `service_id` int(11) NOT NULL,
  `referrer_id` int(11) NOT NULL,
  `referrer_type` enum('referral','affiliate') NOT NULL,
  `new_user_identifier` varchar(255) DEFAULT NULL,
  `click_count` int(11) DEFAULT 0,
  `conversion_count` int(11) DEFAULT 0,
  `discount_or_commission` decimal(10,2) DEFAULT 0.00,
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 6. Service Clicks Tracking
CREATE TABLE IF NOT EXISTS `service_clicks` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `service_id` int(11) NOT NULL,
  `referrer_id` int(11) NOT NULL,
  `referrer_type` enum('referral','affiliate') NOT NULL,
  `ip_address` varchar(45),
  `user_agent` text,
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
